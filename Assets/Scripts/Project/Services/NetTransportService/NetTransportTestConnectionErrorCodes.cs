namespace Project.Services.NetTransportService
{
    public enum NetTransportTestConnectionErrorCodes
    {
        ConnectionFailed,
        HostNotFound,
        PortNotFound,
        InvalidHost,
        InvalidPort
    }
}